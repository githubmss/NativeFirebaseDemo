<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mss.firebasedummy">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FireBaseDatabase.activity.CreateUserActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:label="Login"
            android:screenOrientation="portrait"></activity>

        <activity
            android:name=".LoginActivity"
            android:label="Login"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".RegisterActivity"
            android:label="Register"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".HomeActivity"
            android:label="Home"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".ForgotPasswordActivity"
            android:label="ResetPassword"
            android:screenOrientation="portrait"></activity>

        <activity
            android:name=".UpdateProfileActivity"
            android:label="Update Profile"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".FireBaseDatabase.activity.UpdateUserActivity"
            android:label="Update User"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".FireBaseDatabase.activity.UsersListActivity"
            android:label="Get Users"
            android:screenOrientation="portrait"></activity>


    </application>

</manifest>